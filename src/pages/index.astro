---
// Fetch de dados da API no build time
const response = await fetch('https://jsonplaceholder.typicode.com/posts?_limit=5');
const posts = await response.json();
---

<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meu Site Jamstack</title>
    <style>
        body {
            font-family: system-ui, -apple-system, sans-serif;
            max-width: 800px;
            margin: 0 auto;
            padding: 2rem;
            line-height: 1.6;
        }
        .post {
            background: #f5f5f5;
            padding: 1.5rem;
            margin: 1rem 0;
            border-radius: 8px;
        }
        h1 {
            color: #333;
        }
        h2 {
            color: #666;
            font-size: 1.2rem;
        }
    </style>
</head>
<body>
    <h1>🚀 Meu Site Jamstack no GitHub Pages</h1>
    <p>Este site foi construído com Astro e usa a API JSONPlaceholder</p>
    
    <h2>📝 Posts Recentes:</h2>
    {posts.map(post => (
        <div class="post">
            <h3>{post.title}</h3>
            <p>{post.body}</p>
        </div>
    ))}
</body>
</html>